// 产品版本与构建号配置。发布 DEV/STA 版本时修改 config/version.js。
const DAMUKU_VERSION = {
    productVersion: 'DEV 0.1.4',
    buildId: '20260817-2'
};

if (typeof module !== 'undefined') module.exports = DAMUKU_VERSION;
