# Damuku_music 使用说明

这是一个可以嵌入 OBS、直播姬或其他浏览器源的 B 站直播点歌工具。观众在直播间发送弹幕点歌，插件自动查询歌曲并播放。

## 一、安装和启动

### 环境要求

- Node.js 18 或更高版本
- npm
- PM2（项目依赖中已经包含）

### 安装依赖

在项目根目录执行：

```bash
npm install
```

### 启动服务

```bash
npm start
```

默认地址：

```text
http://localhost:8000/order/
```

常用命令：

```bash
npm run log       # 查看日志
npm run restart   # 修改服务端代码后重启
npm run stop      # 停止服务
```

首次启动时，如果配置文件不存在，程序会自动从 `config/default` 创建配置。

## 二、OBS 配置

在 OBS 中添加“浏览器”来源，网址填写：

```text
http://localhost:8000/order/?roomid=直播间号
```

例如：

```text
http://localhost:8000/order/?roomid=4646297
```

建议浏览器源尺寸设置为：

- 宽度：420
- 高度：300～600
- 自定义 CSS：根据直播画面需要设置

页面背景默认透明，只有点歌卡片和队列会显示在直播画面中。

## 三、直播模式和浏览器预览

### OBS 直播模式

直播模式默认开启，只显示观众需要看到的点歌队列：

```text
http://localhost:8000/order/?roomid=4646297&source=obs
```

实际上不写 `livemode` 时也默认为 `true`：

```text
http://localhost:8000/order/?roomid=4646297&source=obs
```

直播模式由 OBS 页面负责，画面只展示点歌队列列表，不展示播放器卡片和控制按钮：

- 获取 B 站弹幕
- 加载空闲歌单
- 播放音乐
- 处理点歌队列

### 浏览器预览模式

自己在浏览器中查看完整播放界面时，使用：

```text
http://localhost:8000/order/?roomid=4646297&livemode=false&source=control
```

此页面是 OBS 播放页的镜像：

- 不会再次连接 B 站弹幕
- 不会再次加载随机歌单
- 不会创建第二个音频播放器
- 会同步 OBS 页面当前歌曲和点歌队列
- 播放控制按钮会把启用声音、暂停、切歌和音量操作发送给 OBS 播放页

建议使用启动器生成的一组 OBS 播放页面和控制页面。服务端保存房间唯一状态，不依赖两个页面是否属于同一个浏览器进程；无论打开多少个 OBS 链接或控制链接，当前歌曲、点歌队列、空闲歌单和播放状态都应保持一致。只有第一个 OBS 页面负责真正播放音频，后续 OBS 链接会自动成为监控页。

网址参数之间应该使用 `&`：

```text
正确：?roomid=4646297&livemode=false
```

程序也兼容误写的形式：

```text
?roomid=4646297?livemode=false
```

## 四、全局设置

设置页面地址：

```text
http://localhost:8000/order/settings.html
```

设置页不需要填写 `roomid`，配置保存在当前浏览器中，对所有直播间生效。

主页面点击“设置”按钮会在新窗口打开设置页，不会停止当前音乐。

### 登录设置

- 选择网易云音乐或 QQ 音乐
- 网易云音乐支持二维码登录
- QQ 音乐需要填写 Cookie
- 配置空闲歌单 ID

空闲歌单会在没有观众点歌时自动播放。

### 点歌设置

可以设置：

- 单个用户最大点歌数量
- 全局最大点歌数量
- 单首歌曲最大时长
- 超时限播时长
- 用户历史记录
- 歌曲历史记录
- 用户黑名单
- 歌曲黑名单

### 显示设置

支持：

- 深色主题
- 白色主题
- 播放界面透明度
- 背景模糊程度
- 播放音量（仅在 `livemode=false` 预览页显示）
- 自定义 CSS

音量默认为 50%。在浏览器预览页拖动音量条，会把音量指令同步给 OBS 播放页；预览页本身不播放第二份音乐。

### 直播模式显示内容

可以控制观众在 `livemode=true` 页面看到的内容：

- 当前播放卡片
- 播放控制按钮
- 队列标题和数量
- 点歌人
- 提示消息

默认只显示点歌队列、队列标题和点歌人，不显示播放器按钮和提示消息。

## 五、点歌指令

### 点歌

```text
点歌歌曲名
```

例如：

```text
点歌起风了
```

指定音乐平台：

```text
点歌wy起风了
点歌qq起风了
```

其中：

- `wy`：网易云音乐
- `qq`：QQ 音乐
- 不写平台时默认使用网易云音乐

### 切歌

```text
切歌
```

普通观众只能切换自己点的歌曲；管理员可以切换任意歌曲。

### 暂停和播放

```text
暂停
播放
```

暂停和播放通常只允许管理员操作。

## 六、设置管理员 UID

可以在网址中加入主播 UID：

```text
http://localhost:8000/order/?roomid=4646297&uid=主播UID
```

例如：

```text
http://localhost:8000/order/?roomid=4646297&uid=106307213
```

`uid` 会用于识别管理员的“切歌”“暂停”“播放”指令。

## 七、B 站弹幕连接方式

插件使用 B 站普通直播弹幕接口，不依赖直播开放平台的互动玩法许可。

程序会自动完成：

1. 解析短房间号和真实房间号
2. 创建并复用游客 Cookie 会话和 `buvid3`
3. 获取 WBI 签名、弹幕鉴权 Token 和服务器列表
4. 在 Node 服务端连接 B 站弹幕服务器
5. 按真实房间号复用唯一上游连接
6. 解析 Brotli/Zlib 实时弹幕，并在短断线后用最近历史补偿去重

接收公开弹幕不要求登录。浏览器地址中不需要、也不应该填写 B 站 Cookie、Token 或弹幕服务器地址。

默认使用历史弹幕轮询方式处理点歌，适合当前项目的稳定使用方式。

要使用修复后的实时 WebSocket 模式，追加：

```text
http://localhost:8000/order/?roomid=4646297&realtime=1&debug=1
```

## 八、调试模式

追加 `debug=1`：

```text
http://localhost:8000/order/?roomid=4646297&debug=1
```

然后打开浏览器开发者工具 Console，可以查看：

- B 站共享 WebSocket 连接、认证和重连状态
- 当前真实房间号、连接 ID 和匿名/登录会话类型（不显示 Cookie 或 Token）
- 每 10 秒汇总的上游包、内层包、普通弹幕、解析错误和半包余量
- 历史弹幕解析结果
- 收到的点歌弹幕
- 网易云搜索、歌单和歌曲播放地址请求（不会输出 Cookie 或完整音频 URL）
- 音频加载、元数据、播放、暂停、等待、错误事件
- 控制页发送指令、OBS 页面收到指令及 HTTP 同步结果

## 九、自定义 CSS

在设置页的“显示设置”中填写 CSS，点击“应用 CSS”。

常用选择器：

```css
.playerCard       /* 当前播放卡片 */
.queueCard        /* 点歌队列卡片 */
.playerActions    /* 播放控制按钮 */
.orderTable       /* 点歌列表 */
.alertBox .text   /* 提示框 */
```

示例：

```css
/* 缩小圆角 */
.playerCard,
.queueCard {
    border-radius: 8px;
}

/* 修改按钮大小 */
.playerActions button {
    font-size: 16px;
}

/* 隐藏点歌人 */
.orderTable th:nth-child(3),
.orderTable td:nth-child(3) {
    display: none;
}
```

自定义 CSS 会保存在浏览器本地，对当前浏览器中的所有直播间生效。

## 十、常见问题

### 1. 点击设置后音乐停止了

新版设置会在新窗口打开。如果浏览器拦截弹出窗口，请直接访问：

```text
http://localhost:8000/order/settings.html
```

### 2. OBS 和浏览器显示的歌曲不一样

请确认：

- OBS 使用启动器生成的 `source=obs` 链接
- 浏览器使用启动器生成的 `livemode=false&source=control` 链接
- 两边使用相同的 `roomid`
- OBS 页面已经先启动
- 服务端已经重启到最新代码

### 3. 没有声音

浏览器可能拦截自动播放。打开完整页面后点击一次“启用声音”。

OBS 中还需要检查：

- 浏览器源是否启用了音频
- OBS 音频监视器是否设置正确
- 系统输出设备是否正常

### 4. 没有获取到弹幕

更新实时弹幕代码后，必须先关闭原来的服务终端，再重新运行启动器。启动器检测到旧服务仍在运行时会直接复用旧进程，仅刷新网页不会加载新的 Node 后端代码。

先确认直播间正在开播。检查默认历史模式时使用：

```text
http://localhost:8000/order/?roomid=直播间号&debug=1
```

检查实时模式时使用：

```text
http://localhost:8000/order/?roomid=直播间号&realtime=1&debug=1
```

查看 Console 是否出现 `upstream authenticated`。注意：认证成功只表示 B 站接受了连接，不能单独证明普通聊天弹幕已经下发。也可以访问下面的无敏感信息指标接口：

```text
http://localhost:8000/order/bili-api/live/metrics?room_id=直播间号
```

重点检查：

- `commandCountByType.DANMU_MSG`：B 站上游实际下发的普通聊天弹幕数；
- `danmuDecodedCount`：成功标准化的弹幕数；
- `proxySentCount` 和 `subscriberDeliveryCount`：发送到本地页面的数量；
- `parseErrorCount` 应为 0，`remainderBytes` 不应持续增长；
- `httpAckFailureCount` 不应持续增长。

公开直播间接收弹幕不要求登录。游客流中的 UID 可能为 0、昵称可能打码；程序会用 `user_hash` 生成仅供本地点歌去重和限额使用的负数伪 UID，这不代表弹幕漏收。

### 5. Chrome 和 Firefox 能同步，但 Bilibili 直播姬不能同步

直播姬使用独立的内置 WebView，不共享 Chrome 或 Firefox 的 localStorage，也可能对 `localhost` 回环地址解析异常。同步状态实际通过本地服务接口完成，因此请按下面方式配置：

- 重新打开启动器，使用它生成的 `127.0.0.1:8000` OBS 链接。
- 直播姬中使用带 `source=obs` 的 OBS 链接，不要放控制页链接。
- 修改 `webapi.js` 或前端代码后，在直播姬里删除旧浏览器源，再重新添加链接，避免 WebView 缓存旧脚本。
- 如果 `127.0.0.1` 仍无法访问，把 `config/config.yaml` 的 `web_server_host` 改为 `0.0.0.0`，重启服务后使用电脑 IPv4 地址，例如 `http://192.168.1.100:8000/order/?roomid=房间号&source=obs`。

### 5. 修改代码后页面没有更新

浏览器执行 Ctrl+F5 强制刷新。修改服务端代码后重启 PM2：

```bash
npm run restart
```
